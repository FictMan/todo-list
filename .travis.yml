language: ruby

services:
  - postgresql

before_script:
  - gem install bundler
  - bundle install
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  bundle exec rubocop:
    - config: ".rubocop.yml"
  - bundle exec rspec spec
